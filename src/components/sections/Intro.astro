---
export interface Props {
  title?: string;
  heading: string;
}

const { title = false, heading } = Astro.props;
---

<div class="intro">
  {title && <p class="intro_title">{title}</p>}
  <h2 class="intro_heading">{heading}</h2>
  <slot />
</div>

<style>
  .intro {
    width: 80%;
    text-align: center;
  }

  .intro_title {
    font-size: 1.8rem;
    font-weight: 800;
    text-align: center;
    color: var(--accent-color);
    text-transform: uppercase;
    margin-bottom: 20px;
  }

  .intro_heading {
    margin-bottom: 20px;
  }

  .intro_text {
    margin: 20px 0;
    text-align: center;
    /* font-size: 1.3rem; */
  }

  .intro_img {
    display: flex;
    justify-content: center;
    padding: 30px 0;
    max-width: 1000px;
    margin: auto;
    /* max-height: 450px; */
  }

  .intro_img img {
    object-fit: contain;
  }

  .intro--contact {
    height: 10vh;
    padding-bottom: 20px;
    margin-bottom: 20px;
  }

  .intro_grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    align-items: start;
    justify-content: space-around;
    width: 100%;
  }
</style>
